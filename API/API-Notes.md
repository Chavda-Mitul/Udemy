# API is the set of modules function and code that the programmer can use to create the software.
# Endpoints : is a like that we can access the data of the api
# Paths : Is the catory of the api at the last of the link.
# Parameters : This comes after the Path and stat with the ?. We can say is it a quarry the filter out the data.
# Autentication :  Everytime you make the request to api they need to know who is using the api and for how much call they are making. 
